type Person {
  id: ID! @unique
  firstName: String!
  lastName: String!
  email: String
  phoneNumber: String!
  reserved: Boolean!
  reservedAt: DateTime
}

type Family {
  id: ID! @unique
  members: [Person!]!
}
