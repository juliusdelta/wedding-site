type Person {
  id: ID! @unique
  firstName: String!
  lastName: String!
  email: String
  phoneNumber: String!
  reserved: Boolean! @default(value: false)
  reservedAt: DateTime
  family: Family
}

type Family {
  id: ID! @unique
  members: [Person!]!
}
