# import * from './generated/prisma.graphql'

type Mutation {
  createPerson(
    firstName: String
    lastName: String
    reserved: Boolean
    email: String
    phoneNumber: String
  ): Person!
}

type Query {
  persons: [Person]!
}
